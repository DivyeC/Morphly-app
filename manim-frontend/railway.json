{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "./node_modules/.bin/serve -s dist -l 8080",
    "restartPolicyType": "ON_FAILURE"
  }
}